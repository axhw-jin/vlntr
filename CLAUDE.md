# vlntr — Claude Code 작업 규칙

봉사 기회를 올리는 쪽과 참여하는 쪽을 잇는 정적 웹사이트.
기획·구현 범위·로드맵은 `prd.md` 를 본다.

## 이 저장소는 PC 두 대에서 번갈아 작업한다

집 PC와 직장 PC를 오간다. **한쪽에서 한 일을 다른 쪽이 모르는 상태로
작업을 시작하면 안 된다.** 아래 두 절차를 반드시 지킨다.

### 1. 세션을 시작할 때 — 먼저 확인하고 사용자에게 요약해 알린다

```
git fetch origin
git status -sb                # 뒤처져 있으면 pull 을 제안한다
git log --oneline -15         # 지난번 이후 들어온 커밋
```

그다음 **`WORKLOG.md` 맨 위 항목을 읽는다.** 특히 이 두 가지를 본다.

- **저장소 밖 변경** — git에 안 들어오는 것들이다. 이 PC에도 반영됐는지
  확인해야 한다. (Supabase 스키마·RLS·Auth 설정, Vercel 설정, 도메인,
  새로 깔아야 하는 도구, 새로 연결해야 하는 계정)
- **다음 할 일** — 하다 만 작업이 있으면 거기서 이어간다.

확인 결과를 짧게 정리해 사용자에게 알린다. 예:
"직장 PC에서 커밋 3건 들어왔고, Supabase에 `lat`/`lng` 컬럼이 추가됐다.
이 PC에서 따로 할 설정은 없다."

### 2. 커밋·푸시할 때 — `WORKLOG.md` 맨 위에 항목을 추가한다

git 로그가 이미 **코드가 어떻게 바뀌었는지**는 기록한다.
그러니 WORKLOG에는 **git이 담지 못하는 것**만 적는다. 코드 변경을
장황하게 옮겨 적지 말고, 커밋 해시로 가리킨다.

반드시 적을 것:

- **저장소 밖 변경** — Supabase(스키마, RLS 정책, 함수, Auth 설정),
  Vercel(도메인, 환경변수, 보호 설정), 새로 설치한 도구, 계정 연결.
  없으면 "없음" 이라고 명시한다. 비워두지 않는다.
- **다음 할 일** — 하다 만 것, 이어서 할 것
- **판단 근거** — 왜 그 방법을 골랐는지, 어떤 대안을 왜 버렸는지
- **함정** — 다음 사람이 같은 데서 시간을 버리지 않도록

형식은 `WORKLOG.md` 의 기존 항목을 그대로 따른다. 새 항목이 항상 맨 위다.

## 로컬에서 돌려보기

`package.json` 도 빌드도 없는 정적 파일 묶음이다. 다만 각 페이지가
`<script type="module">` 을 쓰므로 `file://` 로 열면 안 되고 서버가 필요하다.

```
npx serve .        # 또는 아무 정적 서버나
```

Google 로그인을 로컬에서 시험하려면 Supabase Auth의 Redirect URLs에
그 주소가 등록돼 있어야 한다 (`http://localhost:3000/**` 는 등록돼 있음).
포트를 바꾸면 그 주소도 등록해야 로그인이 돌아온다.

## 구조 메모

- 페이지마다 HTML 한 장에 스타일과 스크립트가 같이 들어 있다.
  프레임워크도 번들러도 쓰지 않는다. 이 방식을 유지한다.
- 여러 페이지가 함께 쓰는 것만 따로 뺀다:
  `theme.css`(색 토큰), `theme.js`(라이트/다크), `datepicker.js`(날짜 선택기)
- 공용 CSS는 페이지의 인라인 `<style>` 보다 먼저 읽히므로, 우선순위가
  밀리지 않게 `button.dp-field` 처럼 **요소+클래스**로 적는다.
- Supabase URL과 anon key는 HTML에 그대로 들어 있다. 비밀이 아니고
  RLS로 막는다. Naver 지도 Client ID도 마찬가지로 도메인으로 막는다.
- 쓰기는 전부 RPC 함수를 거친다 (`create_opportunity`, `apply_to_opportunity` 등).
  테이블에 직접 insert/update 하지 않는다.
