# 작업 기록

PC 두 대를 오가며 작업하므로, 커밋할 때마다 **맨 위에** 항목을 추가한다.
코드 변경은 git 로그가 이미 기록하니, 여기에는 **git이 담지 못하는 것**을 적는다.
규칙은 `CLAUDE.md` 참고.

---

## 2026-08-18 (7) · 직장 PC

**커밋** 이 커밋 (배경을 가로로 흐르는 사진 띠로, eyebrow 두 줄 폭 맞춤)

### 한 일

- 회전 링을 버리고 **오른쪽으로 흐르는 가로 띠**로 바꿨다. 사진을 크게(520px 폭,
  히어로 높이 전체) 키우고 진하기도 올렸다(라이트 0.5 / 다크 0.38).
- eyebrow 의 `volunteer` / `platform` 두 줄 좌우 폭을 정확히 맞췄다.

### 저장소 밖 변경

- 없음.

### 다음 할 일

- 로드맵 2번 **지도 보강** (`lat`/`lng` 컬럼). 스키마 변경이라 Supabase 접근 필요.
- `map.html` 왼쪽 목록에 필터를 붙일지 결정

### 알아둘 것

- **띠는 같은 사진 묶음을 두 벌 이어 붙이고 `-50% → 0` 으로 민다.** 이때 사진 사이를
  `gap` 으로 띄우면 **두 벌 사이의 간격이 하나 모자라** 전체 폭의 절반이 한 벌의 폭과
  간격의 절반만큼 어긋난다. 한 바퀴마다 6px 씩 튀었다. `margin-right` 로 주면
  간격이 사진마다 딸려 있어 폭이 정확히 두 배가 되고 이음매가 사라진다.
  실제로 절반(6384px)과 한 벌(6384px)이 일치하는 것을 재서 확인했다.
- **두 줄의 폭을 맞추는 방법**: 글자 하나하나를 `<i>` 로 쪼개고 줄마다
  `display:flex; justify-content:space-between` 을 준다. 그러면 짧은 쪽(platform)이
  긴 쪽(volunteer)의 폭까지 저절로 늘어난다. letter-spacing 을 손으로 맞추는 방법은
  글꼴이 바뀌면 어긋나지만 이 방법은 어떤 글꼴에서도 맞는다(측정: 둘 다 74.91px).
  글자를 쪼갠 만큼 스크린리더가 한 자씩 읽지 않도록, 보이지 않는 `.sr-only` 에
  온전한 문구를 두고 쪼갠 글자는 `aria-hidden` 처리했다.
- 글자 대비는 `.hero-veil` 의 **가로 방향 그라데이션 하나**로 지킨다. 왼쪽 40%까지
  카드색으로 덮고 76% 지점에서 사라진다. 위아래는 덮지 않는다 — 사진이 히어로
  끝까지 닿아야 하기 때문이다.

### 검증

- 이음매 계산을 실측(전체 12768px / 절반 6384px = 한 벌 6384px)
- eyebrow 두 줄 폭 실측(74.91px 동일)
- 라이트·다크 양쪽에서 글자 가독성 확인

---

## 2026-08-18 (6) · 직장 PC

**커밋** 이 커밋 (히어로 배경을 회전하는 사진 링으로, eyebrow 두 줄로)

### 한 일

- 오른쪽에 모아 두던 사진 3장을 없애고, **글자 뒤에서 천천히 도는 사진 링**으로 바꿨다.
  사진은 12장으로 늘렸다.
- eyebrow 를 `volunteer` / `platform` 두 줄로 바꿨다.

### 저장소 밖 변경

- 없음. 사진은 전부 Unsplash 직접 링크라 저장소에 파일이 늘지 않는다.

### 다음 할 일

- 로드맵 2번 **지도 보강** (`lat`/`lng` 컬럼). 스키마 변경이라 Supabase 접근 필요.
- `map.html` 왼쪽 목록에 필터를 붙일지 결정

### 알아둘 것

- **한 요소에 정지 transform 과 애니메이션 transform 을 같이 못 준다.** 나중 것이
  앞의 것을 통째로 덮는다. 그래서 층을 넷으로 나눴다:
  `.ring`(회전) → `figure`(각도·반지름 배치) → `.tile`(figure 의 각도를 되돌림)
  → `img`(링과 반대로 회전). 이래야 사진이 돌면서도 늘 똑바로 선다.
  **층을 하나라도 합치면 사진이 거꾸로 뒤집힌다.**
- 사진 주소는 **실제로 200 이 떨어지는지 12장 전부 curl 로 확인**했고, 화면에 띄워
  주제에 맞는지도 눈으로 봤다. Unsplash 검색 결과 중 하늘·머그컵처럼 봉사와
  상관없는 4장은 뺐다. 새 사진을 넣을 때도 같은 확인을 거칠 것.
- 글자 대비는 `.hero-veil` 이 지킨다. 사진 위에 카드색 그라데이션을 두 겹 깔아
  왼쪽·가운데(글자 자리)를 덮고 가장자리로 갈수록 사라지게 했다.
  사진 자체는 `--hero-bg-op`(라이트 0.42 / 다크 0.3) + `blur(1.5px)` 로 죽였다.
- `.wrap` 을 쓰는 요소에 `max-width` 를 다시 주면 가운데로 몰려 제목과 왼쪽 끝이
  어긋난다. `.hero-copy` 에 620px 를 줬다가 겪었다. `.wrap` 의 1040px 를 그대로 쓴다.
- **자동화 브라우저(Claude in Chrome)는 "동작 줄이기"가 켜져 있다.** 그래서 회전이
  멈춰 보인다. 버그가 아니다. 확인하려면 `!important` 로 애니메이션을 덮어써야 한다.

### 검증

- 링과 사진의 회전각이 매 순간 정확히 반대(-52/+52, 0/0, 30/-30)인 것을 측정해,
  사진이 뒤집히지 않는 것을 확인
- 라이트·다크 양쪽에서 글자 대비 확인

---

## 2026-08-18 (5) · 직장 PC

**커밋** 이 커밋 (히어로 제목 한 줄 + 레이아웃 재배치, eyebrow 를 이름 유래로 교체)

### 한 일

- 제목을 반쪽 칸에서 꺼내 **폭 전체를 쓰는 한 줄**로 바꿨다(54px). 그 아래에
  글(왼쪽) + 사진(오른쪽) 두 칸을 둔다.
- eyebrow "봉사 기회 매칭" 을 `volunteer` 로 바꾸고 모음만 흐리게 했다.

### 저장소 밖 변경

- 없음.

### 다음 할 일

- 앞 항목과 같다 (로드맵 2번 지도 보강).

### 알아둘 것

- **`.wrap` 을 쓰는 요소에 `margin` 단축 속성을 주면 안 된다.** `.wrap` 의
  `margin: 0 auto` 가 통째로 덮여 가운데 정렬이 풀린다. 실제로 `.hero-head` 에
  `margin: 0 0 36px` 를 줬다가 제목이 화면 왼쪽 끝에 붙었다. `margin-bottom` 만 쓴다.
- 제목이 위로 빠지면서 왼쪽 칸이 짧아졌다. `.hero-grid` 를 `align-items: center`
  로 두면 제목과 설명 사이가 100px 넘게 벌어진다. `start` 로 바꾸고 사진 묶음
  높이를 480px → 430px 로 낮춰 빈 곳을 줄였다.
- eyebrow 의 `<i>` 는 기울임이 아니라 **모음 표시용**이다
  (`font-style: normal` 로 되돌려 놨다). `volunteer` 에서 `o·u·ee` 를 흐리게 하면
  또렷하게 남는 글자가 v·l·n·t·r 이다. 읽는 사람에게 이름의 유래를 말로 설명하지 않는다.
  스크린리더에는 `aria-label="volunteer"` 로 온전한 단어가 읽힌다.
- 좁은 화면에서는 제목이 한 줄에 안 들어가 두 줄로 접힌다. 가운데 정렬이라
  넓은 화면에서 문제였던 "윗줄만 길어 보이는" 느낌은 없다.

### 검증

- 세 줄 안 → 한 줄 안으로 바꾸는 과정에서 넓은 화면 배치를 브라우저로 확인
- eyebrow 확대해서 v·l·n·t·r 이 또렷하게 읽히는지 확인

---

## 2026-08-18 (4) · 직장 PC

**커밋** 이 커밋 (히어로 제목 3줄, 설명 둘째 문장 교체)

### 한 일

- 제목을 `봉사자와 / 피봉사자를 / 잇습니다.` 세 줄로 바꾸고 46px 로 키웠다.
- 설명 둘째 문장을 사용자가 지정한 문장으로 바꿨다.

### 저장소 밖 변경

- 없음.

### 다음 할 일

- 앞 항목과 같다 (로드맵 2번 지도 보강).

### 알아둘 것

- 두 줄(`봉사자와 피봉사자를 / 잇습니다.`)은 윗줄 9자·아랫줄 5자라 위가 무겁고
  아래가 뚝 끊겨 보였다. 세 줄은 4·5·5자로 길이가 고르다.
  **한 줄로 붙이는 안도 시험했는데**(33px), 폭이 정확히 504px 로 칸을 꽉 채워
  여백이 사라지고 아래 설명 문구와 길이가 비슷해져 밋밋했다. 그래서 버렸다.
- 줄이 짧아진 만큼 40px → 46px 로 키웠다. 오른쪽 사진 묶음(480px)과 높이가 맞는다.
  좁은 화면은 30px 그대로다(세 줄이라 높이는 늘지만 넘치지 않는다).

### 검증

- 브라우저에서 세 안(2줄·1줄·3줄)을 실제로 그려 비교한 뒤 3줄을 골랐다

---

## 2026-08-18 (3) · 직장 PC

**커밋** 이 커밋 (랜딩 히어로 문구·줄바꿈 손질)

### 한 일

- 제목 줄바꿈을 `봉사자와 피봉사자를 / 잇습니다.` 로 바꾸고, 설명 문구를 다시 썼다.

### 저장소 밖 변경

- 없음.

### 다음 할 일

- 앞 항목과 같다 (로드맵 2번 지도 보강).

### 알아둘 것

- 한글은 기본값(`word-break: normal`)으로 두면 **낱말 중간에서 줄이 끊긴다.**
  실제로 "멀 / 리 갈 것 없이" 로 끊겼다. 히어로의 `h1` 과 `.lede` 에
  `word-break: keep-all` 을 넣어 낱말 사이에서만 접히게 했다.
  **다른 화면의 긴 한글 문장도 같은 문제가 있다.** 눈에 띄면 같은 속성을 넣으면 된다.
- 제목의 `<br>` 는 넓은 화면용이다. 좁아지면 keep-all 이 낱말 사이에서 한 번 더 접어
  세 줄(`봉사자와 / 피봉사자를 / 잇습니다.`)이 된다. 폭 300px 로 줄여 확인했다.

### 검증

- 브라우저에서 넓은 화면(2줄)과 폭 300px(3줄) 양쪽 확인. 낱말 중간 끊김 없음

---

## 2026-08-18 (2) · 직장 PC

**커밋** 이 커밋 (등록 폼 시/도 선택 · 상단 내비게이션 · 랜딩 문구)

### 한 일

- **등록 폼의 위치**를 시/도 선택 + 상세 주소로 나눴다. 저장되는 `location` 문자열
  형식("서울 마포구 성산동")은 그대로라 **스키마도 기존 데이터도 손대지 않았다.**
- **상단 내비게이션을 `nav.js` 로 통일**했다. 화면마다 헤더가 달라서 이동 방법이
  제각각이던 것을 하나로 모았다.
- 랜딩 히어로 문구를 바꿨다.

### 저장소 밖 변경

- **없음.** Supabase · Vercel · NCP 모두 변경 없다.
  (NCP 에 `http://localhost:3000` 을 추가한 것은 앞 항목에 적었다)

### 다음 할 일

- 로드맵 2번 **지도 보강** (`lat`/`lng` 컬럼). 스키마 변경이라 Supabase 접근 필요.
- `map.html` 왼쪽 목록에 필터를 붙일지 결정
- `list.html` 의 `REGION_ALIAS` 는 이제 `regions.js` 에 있다. 등록 폼이 시/도를
  고르게 됐으니, **앞으로 새로 들어오는 데이터에는 별칭 표가 필요 없다.**
  예전 데이터를 정리하면 표를 지울 수 있다.

### 알아둘 것

- **`nav.js` 는 supabase 를 import 하지 않는다.** supabase-js 는 esm.sh 에서
  받아오므로 같이 묶으면 내비게이션이 네트워크를 기다렸다 그려진다. 대신 각 화면이
  준비되는 대로 `window.vlntrNav.setAuth(user, signIn, signOut)` 을 부른다.
  화면마다 `<script type="module">` 이 두 개인 이유가 이것이다 (앞은 내비게이션 전용).
- 내비게이션이 테마 버튼을 다시 그리므로, `theme.js` 가 `window.vlntrTheme.mount()`
  를 열어두고 `nav.js` 가 그린 직후 부른다. 중복 바인딩은 `data-theme-bound` 로 막는다.
- **화면 제목은 더 이상 링크가 아니다.** 마이페이지에서 제목을 누르면 홈으로 가던
  동작을 없앴다. 홈은 왼쪽 로고 하나로만 간다.
- `map.html` 의 `.split` 높이 `calc(100vh - 162px)` 는 새 헤더(58px) 기준이다.
  헤더 높이를 바꾸면 이 값도 같이 바꿔야 한다.
- 위치 문자열은 여전히 자유 문자열이다. 시/도만 고르게 했을 뿐 상세 주소는 자유 입력이라,
  예전처럼 시/도 없이 저장된 데이터도 수정 모드에서 열린다
  (`splitLocation()` 이 시/도를 못 알아보면 전부 상세 주소로 넣고 시/도는 비운다).

### 검증

- 브라우저(localhost:3000)에서 5개 화면 전부 확인 — 내비게이션 렌더, 현재 화면 표시,
  라이트/다크 전환, 네이버 지도 로드(3000 포트 등록 확인됨), 시/도 17개 채워짐
- 모듈 구문 검사: 각 HTML 의 `<script type="module">` 블록 전부 `node --check` 통과
- **로그인이 필요한 경로는 코드로만 확인**했다 — 수정 모드 프리필, 로그아웃 버튼,
  로그인 후 나타나는 등록·마이페이지 링크

---

## 2026-08-18 · 직장 PC

**커밋** 이 커밋 (등록 폼 날짜 입력을 달력으로 교체)

### 한 일

- `new.html` 의 `<input name="date" type="date">` 를 `datepicker.js` 로 바꿨다.
  이제 등록 폼과 목록 필터가 같은 달력을 쓴다.

### 저장소 밖 변경

- **네이버 클라우드 플랫폼** — Application ▸ Web 서비스 URL 에
  `http://localhost:3000` 을 추가했다. 기존에는 `http://localhost:4173` 만 있어서
  `CLAUDE.md` 가 권하는 `npx serve .`(3000) 로 띄우면 `map.html` 이 네이버 인증에
  실패하고 OpenStreetMap 으로 대체됐다. 이제 두 포트 다 네이버 지도로 뜬다.
- **Supabase** — 변경 없음. **Vercel** — 변경 없음.

### 다음 할 일

- 로드맵 2번 **지도 보강** (`lat`/`lng` 컬럼). 스키마 변경이라 Supabase 접근 필요.
  Claude 의 Supabase MCP 는 **읽기 전용**이라 SQL 을 받아 콘솔에서 직접 실행해야 한다.
- `map.html` 왼쪽 목록에 필터를 붙일지 결정 (`list.html` 과 맞출지)

### 알아둘 것

- 달력은 `<button>` 이라 **네이티브 `required` 검증이 사라진다.** 그래서 submit 핸들러
  맨 앞에서 `datePicker.getValue()` 가 비었는지 직접 보고 "날짜를 선택해 주세요." 를
  띄운다. 다른 폼에 달력을 더 붙일 때도 이 검증을 같이 넣어야 한다.
- `<label>` 이 달력 버튼을 감싸고 있다. `button` 은 label 대상이 되는 요소라
  "날짜" 글자를 눌러도 달력이 열린다. 의도한 동작이다.

### 검증

- 브라우저(localhost:3000)에서 달력 열림 · 날짜 선택 · × 로 지우기 확인
- 날짜를 비우고 제출하면 네트워크 요청 전에 막히는 것 확인 (콘솔 오류 없음)
- 수정 모드 프리필(`datePicker.setValue`)은 로그인이 필요해 코드로만 확인했다

---

## 2026-08-17 · 집 PC

**커밋** `f18c168` 배포 저장소명 수정 · `6d6bf82` 지역·날짜 필터 + 날짜 선택기 · 그리고 이 커밋(작업 규칙 문서)

### 한 일

- 로드맵 1번 **지역·날짜 필터**를 `list.html` 에 넣었다. 스키마는 안 건드리고
  브라우저에서 거른다. 조건은 `?region=&from=&to=` 로 주소창에 남는다.
- 날짜 입력을 `<input type="date">` 에서 직접 만든 달력(`datepicker.js`)으로 바꿨다.

### 저장소 밖 변경

- **Supabase** — Auth ▸ URL Configuration ▸ Redirect URLs 에
  `http://localhost:3000/**` 를 추가했다. 로컬에서 Google 로그인을 시험하려면
  필요하다. Site URL은 건드리지 않았다. **스키마·RLS·함수는 변경 없음.**
- **Vercel** — 변경 없음. 확인만 했다. 아래 "알아둘 것" 참고.
- **집 PC에 새로 설치** — git 2.55, gh 2.97, Node 24.19 LTS.
  이 PC에 아무것도 없어서 winget으로 깔았다. 직장 PC에는 이미 있을 테니 할 일 없음.

### 다음 할 일

- 로드맵 2번 **지도 보강** — `opportunities` 에 `lat`/`lng` 컬럼을 두고 등록할 때
  한 번만 좌표를 찾아 저장한다. 지금은 `map.html` 이 볼 때마다 Nominatim으로
  주소를 찾고 브라우저에 캐시한다. **이건 스키마 변경이라 Supabase 접근이 필요하다.**
- `map.html` 왼쪽 목록에는 아직 필터가 없다. `list.html` 과 맞출지 정해야 한다.
- `new.html` 등록 폼의 날짜 입력도 `datepicker.js` 로 바꿀 수 있다. 두 줄이면 된다.

### 알아둘 것

- **Vercel 프로젝트명은 `vlntr_mockdata`** 다. 저장소명(`vlntr`)과 달라서
  대시보드에서 헷갈린다. mock 데이터 쓰던 시절 이름이 남은 것이고 동작은 정상이다.
- 공개 주소는 **`vlntr.vercel.app`** (별칭 `vlntrmockdata.vercel.app`). 둘 다 열린다.
  GitHub API의 `environment_url` 이 알려주는 `...-pv43oanyj-...` 주소는
  **배포별 프리뷰 URL이라 Vercel 로그인 벽이 걸린다.** 이걸 보고 사이트가
  막힌 줄 알고 설정을 뒤졌는데, 프로덕션 도메인은 멀쩡했다. 시간 낭비 주의.
- auto-deploy는 웹훅이 아니라 **GitHub App** 으로 붙어 있다.
  `gh api repos/axhw-jin/vlntr/hooks` 가 0건이어도 정상이다.
  배포 확인은 `gh api repos/axhw-jin/vlntr/deployments` 로 한다.
- `location` 은 자유 입력이라 "서울"과 "서울특별시"가 섞인다.
  `datepicker.js` 가 아니라 `list.html` 의 `REGION_ALIAS` 표에서 합친다.
  나중에 등록 폼에 시/도 선택을 넣으면 이 표는 필요 없어진다.
- `event_date` 는 `"2026-08-22 (토)"` 형태의 **문자열**이다. 날짜 타입이 아니다.
  앞 10자가 ISO라 문자열 비교로 정렬·범위 판정이 된다.

### 검증

- 필터 로직: `list.html` 에서 실제 함수를 꺼내 라이브 Supabase 데이터로 18개 통과
- 날짜 선택기: jsdom으로 29개 통과 (앞뒤 36개월 순회, 선택·삭제, 중복 열림 방지 포함)
