# PRD

핵심기능: 동작 위주. 목적이 있으면 그 목적을 달성하기 위한 동작.

## 목적

봉사 활동을 구하는 사람과 참여하는 사람 매칭

## 예상 사용자

- 봉사 참여자를 구하는 쪽: 기관, 소상공인, 개인 등
- 봉사 참여자: 학생, 봉사활동 하고 싶은 자

## 핵심 기능 (MVP)

**기관은 봉사 기회를 올리고, 참여자는 이름·연락처를 남겨 신청한다.**

- 기관은 활동 이름, 기관명, 위치, 날짜, 시간, 모집 인원을 입력해 기회를 등록한다.
- 각 기회는 목록에서 위치, 날짜, 시간, 모집 인원을 보여준다.
- 참여자는 이름과 연락처를 입력해 신청하고, 취소할 수 있다.
- 모집 인원이 다 차면 마감으로 표시되고, 서버가 초과 신청을 막는다.
- 기관은 신청자 명단(이름·연락처)을 받아 직접 연락할 수 있다.

## 사용자 흐름 (동작 순서)

```
[기관]   기회 등록 (위치/날짜/시간/인원) ─┐
                                          ↓
[참여자] 목록 열기 → 상세 정보 확인 → 이름·연락처 입력 → 신청 완료
```

## 로그인

- 구글 계정으로 로그인 (Supabase Auth OAuth)
- 목록 보기는 로그인 없이 가능. **등록과 신청은 로그인 필수**
- 기회를 올린 사람은 자기 기회 카드에서 신청자 명단(이름·연락처)을 바로 볼 수 있다
- 같은 기회에 중복 신청 불가, 자기가 올린 기회에는 신청 불가

## 구현 범위

- 화면
  - `index.html` — 랜딩 페이지. 서비스 소개, 사용 흐름 4단계, 역할별 진입점.
    현재 열린 기회 수·남은 자리는 실제 DB에서 읽어 표시
  - `list.html` — 기회 목록 + 신청 폼 + (소유자에게) 신청자 명단
  - `new.html` — 기회 등록 폼. `?id=` 를 붙이면 같은 폼이 수정 모드로 동작 (로그인 전에는 잠김)
  - `mypage.html` — 마이페이지. 탭 두 개
    - 내가 올린 기회: 수정 / 숨기기·다시 공개 / 삭제 + 신청자 명단
    - 내 신청 내역: 신청 정보 확인 + 신청 취소
  - `map.html` — 지도. 기회를 핀으로 표시하고, 왼쪽 목록과 서로 연동된다.
    파일 맨 위 `NAVER_CLIENT_ID`에 네이버 클라우드 플랫폼 Client ID를 넣으면
    네이버 지도로, 비워두면 OpenStreetMap(Carto Voyager) 지도로 그린다.
    인증에 실패해도 OpenStreetMap으로 자동 대체되므로 화면이 비지 않는다.
    좌표 컬럼이 아직 없어서 `location` 문자열을 Nominatim으로 그때그때 찾고
    브라우저에 캐시한다. 위치를 못 찾은 기회는 목록 아래에 따로 보여준다
  - `theme.css` / `theme.js` — 각 화면이 공유하는 색 토큰과 라이트/다크 전환.
    처음에는 OS 설정(`prefers-color-scheme`)을 따르고, 헤더의 해·달 버튼으로 바꾸면
    그 선택을 `localStorage`에 저장해 이후 모든 화면에 적용한다
- 데이터: Supabase (프로젝트 `cpkslszdgbdjhwpayocy`)
  - `opportunities` — 봉사 기회. `description`(상세 설명), `hidden`(숨김) 포함.
    읽기는 누구나지만 `hidden`인 글은 소유자에게만 보임. 수정·삭제는 `owner_id` 본인만
  - `applications` — 신청자 이름·연락처. 본인 신청 + 본인이 올린 기회의 신청자만 조회 가능.
    기회가 삭제되면 신청도 함께 삭제됨(FK cascade)
  - `create_opportunity()` / `update_opportunity()` / `set_opportunity_hidden()` /
    `delete_opportunity()` / `apply_to_opportunity()` / `cancel_application()`
    — 모두 `auth.uid()` 확인, 수정 계열은 소유자까지 확인. 로그아웃 상태(anon)에서는 실행 권한 없음
- 배포: Vercel + GitHub(`axhw-jin/vlntr`) 연동. main에 push하면 자동 배포

## 다음 순서 (로드맵)

1. 지역·날짜 필터
2. 지도 보강 — `opportunities`에 `lat`/`lng` 컬럼을 두고 등록할 때 한 번만 찾아 저장
   (지금은 볼 때마다 브라우저가 찾는다). 주소 자동완성도 이때 함께
3. 이후: 신청 승인/거절, 봉사시간 확인서, 알림

## 알려진 이슈

- 초기 시드 4건은 `owner_id`가 없어 명단을 볼 주인이 없음 (그대로 두기로 함)
- 지도의 위치 찾기는 Nominatim 무료 정책(초당 1회)을 따르므로,
  캐시에 없는 기회가 많으면 처음 열 때 몇 초 걸린다
