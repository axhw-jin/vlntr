# PRD

핵심기능: 동작 위주. 목적이 있으면 그 목적을 달성하기 위한 동작.

## 목적

봉사 활동을 구하는 사람과 참여하는 사람 매칭

## 예상 사용자

- 봉사 참여자를 구하는 쪽: 기관, 소상공인, 개인 등
- 봉사 참여자: 학생, 봉사활동 하고 싶은 자

## 핵심 기능 (MVP)

**기관은 봉사 기회를 올리고, 참여자는 이름·연락처를 남겨 신청한다.**

- 기관은 활동 이름, 기관명, 위치, 날짜, 시간, 모집 인원을 입력해 기회를 등록한다.
- 각 기회는 목록에서 위치, 날짜, 시간, 모집 인원을 보여준다.
- 참여자는 이름과 연락처를 입력해 신청하고, 취소할 수 있다.
- 모집 인원이 다 차면 마감으로 표시되고, 서버가 초과 신청을 막는다.
- 기관은 신청자 명단(이름·연락처)을 받아 직접 연락할 수 있다.

## 사용자 흐름 (동작 순서)

```
[기관]   기회 등록 (위치/날짜/시간/인원) ─┐
                                          ↓
[참여자] 목록 열기 → 상세 정보 확인 → 이름·연락처 입력 → 신청 완료
```

## 구현 범위

- 화면
  - `index.html` — 기회 목록 + 신청 폼
  - `new.html` — 기회 등록 폼
- 데이터: Supabase (프로젝트 `cpkslszdgbdjhwpayocy`)
  - `opportunities` — 봉사 기회. anon 읽기만 허용 (직접 쓰기 차단)
  - `applications` — 신청자 이름·연락처. anon 직접 접근 차단
  - `create_opportunity()` — 기회 등록. 필수값·길이·인원 범위를 서버에서 검증
  - `apply_to_opportunity()` / `cancel_application()` — 신청·취소
- 내 신청 내역: 로그인이 없어 신청 토큰(uuid)을 localStorage에 보관. 이 토큰이 있어야 취소 가능
- 배포: Vercel + GitHub(`hwax-js/vlntr`) 연동. main에 push하면 자동 배포

## 다음 순서 (로드맵)

1. 로그인 (Supabase Auth) — 내 신청 내역, 내가 올린 글, 권한 관리
2. 지도 페이지 — `location`에 위경도 추가 + 지도 API 필요
3. 이후: 지역·날짜 필터, 신청 승인/거절, 봉사시간 확인서, 알림

## 알려진 이슈

- 오류 시 `alert()`이 페이지를 블로킹함 (수정 보류 중)
- 기관이 신청자 명단을 볼 화면이 아직 없음 — 현재는 DB에서 직접 조회해야 함
- 등록한 글을 수정·삭제할 수단이 없음 (로그인 도입 시 함께 해결)
- 누구나 익명으로 글을 올릴 수 있어 스팸에 취약함
